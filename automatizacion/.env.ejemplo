# Copia este archivo a `.env` y complétalo. `.env` está en .gitignore: nunca se sube.
#
# Usa una CUENTA DE SERVICIO de solo lectura, no credenciales personales.
# Si el robot corre con la cuenta de una persona, el día que esa persona cambie
# su contraseña la automatización se cae en silencio, y cualquier acción queda
# registrada en GLPI a su nombre.

GLPI_URL=https://www.seti.co/glpi
GLPI_USER=
GLPI_PASSWORD=

# Texto por el que se reconoce la entidad en GLPI. La sonda lo usa para probar
# si se puede filtrar en origen en vez de descargar todos los clientes.
# Las comillas importan: sin ellas el espacio parte el valor en dos.
GLPI_ENTIDAD="ACCION FIDUCIARIA"

# --- AlertOps -----------------------------------------------------------
# La api-key es personal de la cuenta: AlertOps → Administration →
# Subscription Settings. Usa también aquí una cuenta de servicio si AlertOps
# lo permite, no la de una persona (mismo motivo que con GLPI_USER arriba).
AOPS_URL=https://api.alertops.com
AOPS_API_KEY=

# Texto por el que se reconoce el cliente dentro de Topic/Message (AlertOps no
# tiene un campo de entidad: la cuenta es de SETI completo). Sin comillas el
# espacio parte el valor, igual que en GLPI_ENTIDAD.
AOPS_CLIENTE="ACCION FIDUCIARIA"

# --- Copia de resguardo (trazabilidad mensual) ---------------------------
# Carpeta base donde cada extractor deja una copia adicional, intacta, del
# CSV que ya escribió en automatizacion/salida/ — dentro de una subcarpeta
# con el nombre del mes (p. ej. RUTA_ONEDRIVE/Julio/), creada sola si no
# existe. Pensada para una biblioteca de SharePoint sincronizada localmente
# vía OneDrive, para que el archivo de cada corte no dependa de que alguien
# lo reenvíe. Vacía: se omite este paso, sin error.
#
# Debe apuntar YA a la carpeta del año correspondiente (p. ej.
# «.../Informes Mensuales/2026»): el script solo agrega el nombre del mes
# debajo. Al pasar de año, actualizar esta ruta a mano (una sola línea).
RUTA_ONEDRIVE=

# Si RUTA_ONEDRIVE ya tiene un archivo con ese nombre y contenido distinto al
# que se va a escribir, por defecto NO se sobrescribe (se avisa y se conserva
# el existente) — para no borrar en silencio una versión ya revisada. Poner
# en 1 para forzar la sobrescritura de todas formas. Vacía o "0": protegido.
FORZAR_ONEDRIVE=
